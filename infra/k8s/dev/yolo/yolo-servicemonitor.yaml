apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: yolo
  namespace: dev
spec:
  jobLabel: app
  selector:
    matchLabels:
      app: yolo
  endpoints:
    - port: http
      interval: 15s
      path: /metrics
