apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-datasource
  namespace: prod
data:
  datasource.yml: |
    apiVersion: 1

    datasources:
      - name: prometheus
        type: prometheus
        access: proxy
        url: http://prometheus-svc:9090
        isDefault: true
        editable: true
