apiVersion: v1
kind: Service
metadata:
  name: yolo-svc
  namespace: prod
  labels:
    app: yolo
spec:
  selector:
    app: yolo
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
  sessionAffinity: ClientIP
